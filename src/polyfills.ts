/***************************************************************************************************
 * BROWSER POLYFILLS
 */

/** IE11 requires the following for ngClass support on SVG elements */
import 'classlist.js';  // Run `npm install classlist.js --save`

/** IE10 and IE11 requires the following for ngIf/ngFor support on SVG elements */
import 'svgxuse';  // Run `npm install svgxuse --save`

/**
 * Web Animations `@angular/platform-browser/animations`
 * Required for: All Angular Material components
 **/
import 'web-animations-js';  // Run `npm install web-animations-js --save`

/**
 * Zone JS is required by Angular itself.
 */
import 'zone.js';  // Included with Angular CLI.

/***************************************************************************************************
 * APPLICATION IMPORTS
 */

/** Import other polyfills or app-specific code here */
